from scapy.all import *

def process_packet(packet):
    """
    Обработка входящего пакета
    """
    # Вывод содержимого пакета
    print(packet.summary())

# Захватываем и анализируем сетевой трафик
sniff(prn=process_packet, store=0)
